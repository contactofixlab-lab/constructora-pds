export const metadata = {
  title: 'Admin - Constructora PDS',
  description: 'Panel de administración',
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
